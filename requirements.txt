# The NetForge core (server + client) has NO third-party runtime dependencies.
# It uses only the Python standard library (Python 3.8+):
#   argparse, csv, datetime, getpass, json, logging, os, socket, struct,
#   threading, traceback
#
# Optional (dashboard.py look & feel — falls back to plain ANSI if missing):
rich>=13.0
# Optional (richer host stats in the dashboard; stdlib fallback otherwise):
psutil>=5.9
#
# Development / testing only:
pytest>=7.0
